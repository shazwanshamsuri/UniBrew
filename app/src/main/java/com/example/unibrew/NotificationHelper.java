package com.example.unibrew;

import android.content.Context;

public class NotificationHelper {

    // KEPT EMPTY: This prevents the app from crashing if MainActivity tries to call it.
    public static void createNotificationChannel(Context context) {
        // Do nothing. Feature removed.
    }

    // KEPT EMPTY: This fixes the "builder" error and stops the notification.
    public static void sendProximityNotification(Context context, Cafe cafe) {
        // Do nothing. Nearby notification removed.
    }
}